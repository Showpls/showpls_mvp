version: "3.9"
services:
  app:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./dist:/app/dist:ro
      - ./attached_assets:/app/attached_assets:ro
    networks:
      - showpls-net

  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - ./dist/public:/app/dist/public:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - showpls-net

networks:
  showpls-net:
    driver: bridge
